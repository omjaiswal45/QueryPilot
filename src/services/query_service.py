"""Entry point business logic layer — takes a question + user, runs the graph, persists history.

TODO:
def handle_question(user, raw_question: str) -> dict:
    # 1. build initial AgentState
    # 2. invoke orchestrator.graph
    # 3. write to repository/chat_history_repository.py and query_audit_repository.py
    # 4. return a response DTO (never raw DB rows)
"""
